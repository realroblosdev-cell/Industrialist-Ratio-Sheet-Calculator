name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y python3-pip build-essential git wget unzip openjdk-11-jdk
          pip3 install --upgrade pip
          pip3 install buildozer cython

      - name: Initialize Buildozer (Install Android SDK/NDK)
        run: |
          buildozer android debug deploy run || true
          # The first run will download SDK/NDK and build-tools

      - name: Build APK
        run: buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: bin/*.apk
